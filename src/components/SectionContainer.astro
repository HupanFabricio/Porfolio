---
interface Props {
    id: string;
    class?: string;
}

const { id, class: className } = Astro.props;
---

<section
    id={id}
    class={`relative mx-auto w-full lg:max-w-[740px] ${className}`}
>
    <slot />
</section>
