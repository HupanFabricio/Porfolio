---
import { experienceData } from '../../data';
import { ExperienceItem } from '../components';
import { Briefcase } from '../icons';
---

<div>
    <h2 class="mb-4 flex flex-row items-center gap-x-2 text-2xl font-semibold">
        <Briefcase class="size-6" />Experiencia laboral
    </h2>
    <ol class="relative ms-4 border-s border-gray-200 dark:border-gray-700">
        {experienceData.map((experience) => <ExperienceItem {...experience} />)}
    </ol>
</div>
